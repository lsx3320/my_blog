# 随笔与中转数据维护

## 数据位置与兼容性

- Markdown 文章继续保存在 `src/content/posts`，此版本没有修改或删除文章文件。
- 随笔沿用原 JSONBin，顶层格式仍为数组；中转沿用原 JSONBin，顶层格式仍为 `{ links: [...] }`，写入时保留其他字段。
- 浏览器继续使用 `memo-card:history`、`memo-card:draft`、`memo-card:deleted`、`links:local`，AI 设置与登录状态键名不变。
- 部署仍使用 `https://lushixiao.cn`。不同域名、协议、浏览器与 Cloudflare 预览域名不会共享 localStorage；预览页面的本地空列表不代表生产数据丢失。

## 读写规则

1. 进入列表或阅读页面只读取并合并，不自动向云端写入。
2. 新建、修改或手动同步先读取云端最新版本，再按记录 ID 合并；相同 ID 默认使用云端，明确较新的 `updatedAt` 才覆盖旧版本。
3. 不限制记录数量，不再截断为最近 50 条。
4. 云端读取失败、格式异常或本地备份失败时停止云端写入；页面报告错误，不能把错误响应当成空数据覆盖。
5. 写入前保留原始本地快照及最近五次快照。云端读到的数据另外保存在 `memo-card:cloud:backup:*` 与 `links:cloud:backup:*`。本地备份名称分别为 `memo-card:history:backup:*` 与 `links:local:backup:*`。
6. 中转使用 `deletedIds` 保留跨设备删除标记。随笔保留原本的本地删除标记规则；其他设备的旧副本仍可能在手动同步时恢复被删除的随笔，避免凭空丢弃已有内容。
7. 同一浏览器使用 Web Locks 串行同步（不支持时使用页面内队列）。JSONBin 整体 PUT 不提供本应用所需的跨设备原子比较交换，不能保证不同设备同时编辑时完全无冲突；编辑时建议保持一个写入页面。

## 备份与恢复

- 部署前已只读备份云端：2026-09-07，随笔 21 条，中转 11 条。备份在仓库之外的本机私有目录，包含完整响应与 SHA-256 清单。
- 两个页面新增导出入口，可下载当前浏览器已有的记录；离线记录应在清理浏览器数据之前导出。列表加载完成后再导出以包含最新云端记录。
- `node scripts/backup-cloud.mjs /absolute/private/backup-directory` 可以在部署或维护前创建独立云备份；脚本只 GET，不修改云数据，也不会在日志输出凭据或正文。
- 恢复时先备份最新云端，按记录 ID 合并待恢复内容，再通过同一个 JSONBin 写回；不要直接用空列表或较旧备份覆盖云端。
- 本地原始快照、云端备份与导出文件可能含私人文字，不应提交 Git 或上传公开网站。

## 后续安全升级

现有实现将 JSONBin Master Key 与页面访问密码放在前端，页面遮罩并非真正的服务端访问控制。本次保留原配置以保证兼容性，没有擅自轮换密钥。下一阶段应将 JSONBin 调用移到 Cloudflare Pages Functions，使用服务端环境变量保存密钥并验证登录，再轮换已经公开的旧密钥。完成服务端权限配置之前，不应把当前门禁视为私人数据保护。
