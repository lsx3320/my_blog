# 复制为 .env 并填入真实值（.env 已被 .gitignore 排除，不会提交）
# 云同步（jsonbin.io）—— 建议在 https://jsonbin.io 生成，只用于此 bin
JSONBIN_MASTER_KEY=
JSONBIN_BIN=6a7c55c5f5f4af5e290b8e09

# 可选：AI 整理代理的 DeepSeek key（配置后前端不再需要填 key）
# DEEPSEEK_API_KEY=sk-xxxx
